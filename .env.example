# Copy this file to `.env` and fill in your own values. `.env` is git-ignored.
# Never commit real keys.

# Free Roboflow API key, used by src/download_roboflow_datasets.py to re-pull
# the external datasets. Get one at https://roboflow.com (Settings -> API).
ROBOFLOW_API_KEY=your_roboflow_key_here
